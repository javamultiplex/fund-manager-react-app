export const loginRequestAction = (request) => ({
    type: "LOGIN_REQUST_ACTION",
    request
});

export const loginResponseAction = (response) => ({
    type: "LOGIN_RESPONSE_ACTION",
    response
});
