export const registrationRequestAction = (request) => ({
    type: "REGISTRATION_REQUST_ACTION",
    request
});

export const registrationResponseAction = (response) => ({
    type: "REGISTRATION_RESPONSE_ACTION",
    response
});
